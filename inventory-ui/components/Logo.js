import { Box } from "@chakra-ui/react";
// import { routeHome } from "../../utils/router";

export default function Kali() {
  const home = () => {
    // routeHome();
  };
  return (
    <Box
      display={{
        sm: "block",
        md: "block",
        lg: "block",
        xl: "block",
        "2xl": "block",
      }}
    >
    LGT Inventory
    </Box>
  );
}
